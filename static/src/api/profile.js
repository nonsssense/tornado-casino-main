/**
 * Profile API (prepared).
 *
 * No profile endpoint is exposed by the backend yet.
 * Profile UI uses Telegram initData until GET /api/profile exists.
 */

// Reserved for future GET /api/profile integration.
