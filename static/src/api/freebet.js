/**
 * Freebet API (prepared).
 *
 * Freebet tickets are consumed server-side via GameManager.
 * Dedicated read endpoints will be wired here when exposed.
 */

// Reserved for future:
// GET /api/freebet/available
// GET /api/freebet/tickets
