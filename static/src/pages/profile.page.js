/**
 * Profile page.
 *
 * Profile is rendered as a bottom-sheet overlay — see features/profile/profile.modal.js
 * and overlays/profile.overlay.js.
 */

export function renderProfilePage() {
  // Profile opens via overlayManager.openProfile() from bottom navigation.
  return null;
}
