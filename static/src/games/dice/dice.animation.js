/**
 * Dice roll animation.
 *
 * Responsibility:
 * - Animate the roll using backend-returned `roll` value.
 * - Duration 100–250ms, ease-out, CSS transitions preferred.
 * - Never generate or predict the result — backend is authoritative.
 */

export function animateDiceRoll(roll, container) {
  // TODO: animate to backend-provided roll value
}
