/**
 * Dice UI constants — bet limits may become dynamic later.
 */

export const DICE_BET_LIMITS = {
  min: 0.2,
  max: 20,
};

/** Quick-select amounts shown inside the Bet Amount card. */
export const DICE_QUICK_BETS = [0.1, 0.2, 0.5, 1, 2, 5, 10];
