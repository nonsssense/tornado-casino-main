/**
 * Plinko path animation.
 *
 * Responsibility:
 * - Animate the ball along the exact backend-provided path (`bits`).
 * - Never generate path independently from backend data.
 */

export function animatePlinkoPath(bits, container) {
  // TODO: animate ball through backend path
}
