/**
 * Plinko game feature.
 *
 * Responsibility:
 * - Bet amount, risk mode, rows selection UI.
 * - Submit via game.service.js → POST /api/games/plinco.
 * - Animate exact path from backend `bits` array.
 */

export const PlinkoGame = {
  mount(container) {
    // TODO: implement Plinko board
  },

  unmount() {
    // TODO: cleanup
  },
};
