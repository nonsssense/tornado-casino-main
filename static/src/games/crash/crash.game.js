/**
 * Crash game feature (planned).
 *
 * Responsibility:
 * - Future Crash game UI when backend endpoint exists.
 * - Placeholder until Crash is implemented server-side.
 */

export const CrashGame = {
  mount(container) {
    // TODO: implement when POST /api/games/crash is documented
  },
};
