/**
 * Banner — shared component.
 *
 * Responsibility:
 * - Promotional blocks: image, title, subtitle, CTA.
 * - Content is always dynamic from backend — never hardcode promotions.
 */

export function Banner(options = {}) {
  // TODO: render promotional banner
  return null;
}
