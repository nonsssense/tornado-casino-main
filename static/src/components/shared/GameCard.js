/**
 * GameCard — shared component.
 *
 * Responsibility:
 * - Thumbnail, title, subtitle, RTP, badges, click action.
 * - Acts as a button — no embedded game logic.
 */

export function GameCard(options = {}) {
  // TODO: render game tile from { title, icon, badges, onClick }
  return null;
}
