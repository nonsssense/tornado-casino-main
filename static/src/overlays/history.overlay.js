/**
 * History overlay.
 *
 * Responsibility:
 * - Transaction list: deposits, withdrawals, bets, wins, bonuses.
 * - Data always fetched from backend — never hardcode history entries.
 */

export function HistoryOverlay(options = {}) {
  // TODO: render paginated transaction history
  return null;
}
