/**
 * Withdraw overlay.
 *
 * Responsibility:
 * - Coin, network, address, and amount form.
 * - Submit withdrawal request to backend — no local validation of balance rules.
 */

export function WithdrawOverlay(options = {}) {
  // TODO: render withdraw form when API is documented
  return null;
}
